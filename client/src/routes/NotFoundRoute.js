import React from 'react';

// TODO: this should be made to fit the theme of the site somehow
const NotFoundRoute = () => (
  <div className="NotFound">
    <p>Not found.</p>
  </div>
);

export default NotFoundRoute;
